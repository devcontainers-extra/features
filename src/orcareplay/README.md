# OrcaReplay (via npm) (orcareplay)

Records an AI agent's run at the boundary between the agent and its model provider, then replays that recording so the same run happens again with no provider called.

## Example Usage

```json
"features": {
    "ghcr.io/devcontainers-extra/features/orcareplay:1": {}
}
```

## Options

| Options Id | Description | Type | Default Value |
|-----|-----|-----|-----|
| version | Select the version of OrcaReplay to install. | string | latest |



---

_Note: This file was auto-generated from the [devcontainer-feature.json](devcontainer-feature.json).  Add additional notes to a `NOTES.md`._
